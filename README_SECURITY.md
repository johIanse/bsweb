# 安全加固说明

本版本做了这些基础加固：

- 去掉 compose 中硬编码数据库密码，改为从 `.env` 读取
- 安装页必须携带 `INSTALL_TOKEN` 才可访问
- 安装页默认管理员密码不再预填
- 删除用户改为 POST，并加了基础 CSRF Token
- 增加安全响应头
- 默认关闭错误详情输出（生产建议）

仍建议继续做：

- 仅在内网开放
- 配置 HTTPS / 反向代理
- 增加登录限速
- 增加审计日志分页/导出保护
- 增加更完整的 CSRF 校验覆盖
